
#include "Temperature.pyman.hpp"
#include <boost/python.hpp>
#include "sireunits_headers.h"

using namespace boost::python;
using namespace SireUnits;
using namespace SireUnits::Dimension;

str _pvt_get_name(const Temperature &val){ return str(double(val));}

void register_Temperature_class()
{
    class_< Temperature, bases<Unit> >("Temperature", init<>())
        .def( init<double>() )
        .def( init<const Temperature&>() )
        .def( "__str__", &_pvt_get_name )
        .def( self == self )
        .def( self != self )
        .def( -self )
        .def( self + self )
        .def( self - self )
        .def( self += self )
        .def( self -= self )
        .def( self * other<double>() )
        .def( self / other<double>() )
        .def( other<double>() * self )
        .def( other<double>() / self )
        .def( "to", (double (Temperature::*)(const Temperature&) const) &Temperature::in )
    ;

}

