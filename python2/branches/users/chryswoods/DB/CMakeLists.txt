########################################
#
# CMake file for Python wrapper:_DB
#
########################################

# Third Party dependencies of this module
include_directories( ${BOOST_INCLUDE_DIR}
                     ${PYTHON_INCLUDE_DIR} )

# Sire include paths
include_directories( ${SIRE_INCLUDE_DIR}/SireDB
                     ${SIRE_INCLUDE_DIR} )

# Qt4
include(${QT_USE_FILE})

# Other python wrapping directories
include_directories(${CMAKE_SOURCE_DIR})

# Get the list of autogenerated files
include(CMakeAutogenFile.txt)

# Define the sources in DB
set ( WRAPDB_SOURCES

      _DB.main.cpp

      ${PYPP_SOURCES}

    )

# Create the library that holds all of the class wrappers
add_library (DB ${WRAPDB_SOURCES})

set_target_properties (DB  
                       PROPERTIES
                         VERSION ${SIRE_VERSION}
                         SOVERSION ${SIRE_VERSION_MAJOR}
                         OUTPUT_NAME "DB"
                         PREFIX "_"
                         SUFFIX ${PYTHON_MODULE_EXTENSION}
		       )
		       
target_link_libraries (DB
                       ${SIREDB_LIBRARY}
                       SirePython2
                       ${SIREID_LIBRARY}
                       ${SIREVOL_LIBRARY}
                       ${SIREMATHS_LIBRARY}
                       ${SIREBASE_LIBRARY}
                       ${SIRESTREAM_LIBRARY}
                       ${SIREERROR_LIBRARY}
                       ${BOOST_PYTHON_LIBRARY}
                       ${PYTHON_LIBRARIES}
                       ${QT_QTCORE_LIBRARY}
                      )

# installation
set( INSTALLDIR ${SIRE_PYTHON}/Sire/DB )

install( TARGETS DB
         LIBRARY DESTINATION ${INSTALLDIR}
       )

install( FILES __init__.py
         DESTINATION ${INSTALLDIR} 
       )

